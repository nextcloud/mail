filter:
    excluded_paths:
        - 'vendor/*'
        - 'js/jquery*'
        - 'js/handlebars*'
        - 'js/backbone*'

imports:
    - javascript
    - php

tools:
    external_code_coverage: true

