filter:
    excluded_paths:
        - 'vendor/*'
        - 'js/jquery*'
        - 'js/handlebars*'
        - 'js/backbone*'
        - 'l10n/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage: true

